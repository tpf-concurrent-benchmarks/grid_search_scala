middleware {
    host: "rabbitmq"
    port: 5672
    user: "guest"
    password: "guest"
    queues {
        work: "work"
        results: "results"
    }
}
metrics {
    host: "graphite"
    port: 8125
    prefix: "worker.${WORKER_ID}"
}