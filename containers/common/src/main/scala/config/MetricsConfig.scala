package org.grid_search.common
package config

case class MetricsConfig(
                            host: String,
                            port: Int,
                            prefix: String
                        )
